# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/
*.egg-info/
dist/
build/

# Node
node_modules/
frontend/dist/
frontend/*.tsbuildinfo
frontend/vite.config.ts.timestamp-*.mjs

# Env / local
.env
.env.*
!.env.example

# Editors / OS
.idea/
.vscode/
.DS_Store

# Postgres data (compose volume fallback)
pgdata/

# Visual review artifacts. The snapshot bundles are a few hundred KB each and
# the comparison sheet is emitted alongside them, so the whole directory is
# generated: `node frontend/scripts/snapshot.mjs before|after`.
docs/screenshots/
